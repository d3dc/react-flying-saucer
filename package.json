{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.4.3"
  },
  "dependencies": {},
  "scripts": {
    "lerna": "lerna",
    "postinstall": "lerna bootstrap",
    "build": "lerna run build",
    "format": "lerna run format",
    "clean": "lerna clean"
  }
}
