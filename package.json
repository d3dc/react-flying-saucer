{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.1"
  },
  "dependencies": {},
  "scripts": {
    "lerna": "lerna",
    "postinstall": "lerna bootstrap --hoist",
    "build": "lerna run build",
    "format": "lerna run format",
    "clean": "lerna clean"
  }
}
