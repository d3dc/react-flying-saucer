{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "dependencies": {},
  "scripts": {
    "lerna": "lerna",
    "postinstall": "lerna bootstrap",
    "build": "lerna run build",
    "format": "lerna run format",
    "clean": "lerna clean"
  }
}
